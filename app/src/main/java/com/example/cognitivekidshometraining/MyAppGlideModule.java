package com.example.cognitivekidshometraining;

import android.content.Context;

import com.bumptech.glide.annotation.GlideModule;
import com.bumptech.glide.module.AppGlideModule;

/**
 * Required Glide module to enable Glide's annotation processor to generate necessary code.
 * You don't need to implement any methods unless you need custom configuration.
 */
@GlideModule
public final class MyAppGlideModule extends AppGlideModule {
    // No custom configuration needed for now
}
